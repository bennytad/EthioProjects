@model HRS.Models.RoomTypes

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>RoomTypes</legend>

        @Html.HiddenFor(model => model.room_type_id)

        <div class="editor-label">
            @Html.LabelFor(model => model.room_type_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.room_type_name)
            @Html.ValidationMessageFor(model => model.room_type_name)
        </div>

        @Html.HiddenFor(model => model.HotelID)

        <div class="editor-label">
            @Html.LabelFor(model => model.room_description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.room_description)
            @Html.ValidationMessageFor(model => model.room_description)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
